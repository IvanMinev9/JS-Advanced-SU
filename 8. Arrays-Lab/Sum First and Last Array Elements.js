function sumFirst(nums) {
  // Calculate the sum of the first element (nums[0]) and the last element (nums[nums.length - 1]).
  console.log(nums[0] + nums[nums.length - 1]);
}

sumFirst([20, 30, 40]);
